// babel.config.json
{
  "presets": [
    [
      "@babel/preset-env",    // Include @babel/preset-env for ESNext to ES5 transpilation
      {
        "targets": {
          "node": "current"
        },
        "modules": false
      }
    ],
    "@babel/preset-react"   // Include @babel/preset-react for JSX support
  ]
}

